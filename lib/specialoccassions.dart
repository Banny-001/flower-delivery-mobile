

import 'package:flutter/material.dart';

class SpecialOccasions  extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Special Occassions')),
      body: Center(child: const Text('Special Occassions')),
    );
  }
}

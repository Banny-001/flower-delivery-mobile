import 'package:flutter/material.dart';

class VendorsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Vendors')),
      body: Center(child: const Text('Vendors Content')),
    );
  }
}